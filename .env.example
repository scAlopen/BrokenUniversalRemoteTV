# Environment variables for TV Remote Control

# Database connection (required for production)
DATABASE_URL=postgresql://username:password@hostname:port/database

# Node environment
NODE_ENV=production

# Server port (Render sets this automatically)
PORT=5000